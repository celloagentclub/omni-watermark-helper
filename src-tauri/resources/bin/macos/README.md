# macOS runtime

Run `npm run prepare:runtime:mac` from the repository root to populate this
directory with the local Node.js executable and a redistributable FFmpeg build.
FFmpeg is compiled from the corresponding sources committed under
`resources/third-party`. Generated binaries are intentionally not committed.
