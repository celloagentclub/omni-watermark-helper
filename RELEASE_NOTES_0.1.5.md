# Omni 去水印助手 0.1.5

发布日期：2026-07-12

## 输出档位校正

- 三档参数已实测确认：标准 `CRF 18`、高清 `CRF 10`、无损编码 `CRF 0`。
- 将误导性的“极速 5-8s”改为“标准 CRF 18”。
- 三档共用同一套水印检测、修复和成品复检流程。
- 档位差异主要是最终编码质量、码率和文件体积，不承诺明显速度差异。
- 批次开始后锁定输出档位，避免同一批次混用不同质量参数。
- 预计耗时按实测约 `15 秒/视频` 显示。

## 运行时与分发

- macOS 内置 FFmpeg 更新为本地编译的 `8.1.2` Apple Silicon 版本。
- 保留 `libx264`、PNG 抽帧和系统 zlib 支持，不依赖 Homebrew。
- 构建不使用 `--enable-nonfree`，并在 App 内附带 FFmpeg/x264 许可证、对应源码归档和构建记录。

## 继承 0.1.4

- Tauri 视频与图片处理在 `spawn_blocking` 后台任务运行，界面不再冻结。
- 多数帧持续检测、8 帧成品复检、严格二次修复和失败关闭。
