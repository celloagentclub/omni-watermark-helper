# Omni 去水印助手 0.1.3

发布日期：2026-07-12

## 核心修复

- 删除“未可靠检测到水印时复制原视频并返回成功”的路径。
- 检测必须在多数采样帧的同一位置持续成立，避免字幕和 UI 边缘的单帧误匹配。
- 低置信度定位现在失败关闭，不发布结果。
- 所有视频策略统一执行最终成品复检。
- 成品复检使用 8 个均匀时间采样帧。
- 复检同时检查正向和反向空间相关，能识别黑色反向残影。
- 首次复检失败时自动扩大遮罩并执行严格二次修复。
- 二次复检仍失败时阻止输出。
- Rust 后端强制检查 `verification-status: verified-removed`。
- 视频元信息改由内置静态 FFmpeg 读取，移除运行时 `ffprobe` 依赖和 Rosetta 风险。

## UI 改进

- 视频成功状态改为“已验证”。
- 普通成功显示“成品复检通过”。
- 二次修复成功显示明确提示。
- 失败状态改为“未通过”，失败项不再计入“待处理”。
- 重试摘要只统计本次执行范围。
- 正式桌面 App 隐藏模拟示例入口。
- 浏览器演示明确标记为“演示完成”，不冒充真实复检。
- 移除未接入后端的假高级参数控件。

## 质量边界

本版本不对任意未知水印、任意位置和任意素材承诺数学意义上的 100%。它保证的是：无法可靠确认去除结果时不再显示成功，也不发布新的可用输出。
